import Loader from '@/components/ui/loader';

const Loading = () => (
	<div className="flex h-screen items-center justify-center">
		<Loader size="2xl" className="text-gray-ý00" />
	</div>
);

export default Loading;
